// python_script_editor.tsx (Task 4 stub; Task 5 replaces)
import type { CustomSpec } from "../../api/types";
import type { WidgetContext } from "../widgets/Renderer";
export default function PythonScriptEditor(
  { spec: _spec, ctx: _ctx }: { spec: CustomSpec; ctx: WidgetContext }
) {
  return <div className="text-xs text-fg/50 italic p-2">Python editor (Task 5)</div>;
}
